// Minimal shape of shadcn's `cn` helper, for typechecking the registry file
// outside a consumer project. The real one is installed by `shadcn init`.
export declare function cn(...inputs: Array<string | undefined | null | false | Record<string, boolean>>): string;
